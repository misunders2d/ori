services:
  ori-agent:
    image: ori-agent-image
    build: .
    container_name: ori-agent-daemon
    init: true
    stdin_open: true
    tty: true
    environment:
      - DOTENV_PATH=/code/data/.env
    ports:
      - "${AGENT_PORT:-8000}:8000"
    restart: unless-stopped
    volumes:
      - type: bind
        source: ./data
        target: /code/data
      - type: bind
        source: ./.git
        target: /code/.git
      - type: bind
        source: ./app
        target: /code/app
      - type: bind
        source: ./skills
        target: /code/skills
      - type: bind
        source: ./interfaces
        target: /code/interfaces

  cloudflare-tunnel:
    image: cloudflare/cloudflared:latest
    container_name: ori-cloudflare-tunnel
    command: tunnel --url http://ori-agent:8000
    restart: unless-stopped
    depends_on:
      - ori-agent
