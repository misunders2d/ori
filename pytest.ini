[pytest]
testpaths = tests
python_files = test_*.py
markers =
    infra: tests that require host tools (git, npx, docker) or specific filesystem state — skipped during in-container evolution verification
